import { Meta, Story } from '@storybook/addon-docs';
import { Loader } from './loader';

<Meta title="Utilities/Loading" component={Loader} />

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Loader Component

The loader component is used to show the loading time of the web page. It will be animated until the complete site loading.

<Story name="Loading">
  return <Loader ></Loader>;
</Story>

<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables, mixins, breakpoints from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component</li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
</ul>
