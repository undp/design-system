import { Meta, Story } from '@storybook/addon-docs';
import { ScrollBar } from './ScrollBar';

<Meta title="Utilities/Scrollbars"/>

<style>{`
 .subheading {
    font-weight: 700;
    font-size: 24px;
    color: #031c2d;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
`}</style>

# Scroll Bar Component
The Scroll Bar component defines a scroll on a div or container when the information is too large.

<Story name="Scrollbars">
  return <ScrollBar></ScrollBar>;
</Story>

#### Usage

- Take HTML from the HTML tab in the canvas
- To use SCSS include the following files in your implementation
- Include the common SCSS files like variables, mixin from <code>assets/scss</code>
- Component SCSS file: <code>scrollbar.scss</code>
- To use CSS include the following files in your implementation
- The compiled CSS file from <code>assets/css/style.css</code>