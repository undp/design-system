import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { FrostedImage } from "./FrostedImage";

<Meta title="Utilities/Frosted background" />

# Frosted background
The Frosted Background component is used to give a translucent effect to the background.

### Overview
The Frosted Background component is used to embed an overlay in a web page.

#### When to use:
To create an engaging background.

### Formatting
#### Default
It consists of a glassy background and an image on one side of the page.

### Behaviors
#### States
There is only one state: Default.

<Canvas>
  <Story name="Frosted background">
    return <FrostedImage></FrostedImage> ;
  </Story>
</Canvas>

### CSS and JS References [ADDED BY DEVs]

#### CSS:
Add the base layout style from dist/css/base-minimal.css
- dist/css/components/[component].css

#### JS:
- dist/js/[component].js

### Interactions
- No interaction required with Frosted Background.

### Changelog
1.0 — Released component
