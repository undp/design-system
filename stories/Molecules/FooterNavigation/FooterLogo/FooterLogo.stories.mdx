import { Meta, Story } from '@storybook/addon-docs';
import { FooterLogo } from './FooterLogo';
import logo from '../../../assets/images/logo.svg';
import logowhite from '../../../assets/images/logo-white.svg';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {headerText: ['United Nations',<br/>,'Development Programme']}; return engText;
    case 'arabic': const arabicText = {headerText: ['الأمم المتحدة',<br/>,'برنامج التطوير']}; return arabicText;
    case 'burmese': const burmeseText = {headerText: ['ကုလသမဂ္ဂ',<br/>,'ဖွံ့ဖြိုးရေးအစီအစဉ်']}; return burmeseText;
    case 'japanese': const japaneseText = {headerText: ['国連',<br/>,'開発プログラム']}; return japaneseText;
    default:
      return {headerText: ['United Nations',<br/>,'Development Programme']};
  }
};

<Meta title="Testing/Footer Navigation/Footer Logo" parameters={{
    backgrounds: {
      default: 'white',
      values: [
        { name: 'gray', value: '#D4D6D8' },
      ],
    },
  }} />

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
   .subheading2 {
    font-weight: 700;
    font-size: 14px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
  } 
  .css-zgt7u1 {
    background: #D4D6D8;
  }
  div[id^="story--molecules-footer-navigation-footer-log"] {
    display: inline-block;
    border: 1px solid #000;
    padding: 10px;
    align-items: center;
    margin: 25px 25px 25px 0;
  }
`}</style>


# Footer Logo Component

Footer Logo Component used to display the UNDP or PNUD logo with logo name on the footer section.

<ul>
<li>Two colors variation are created <code>Blue logo with black content</code> and <code>White logo with white content</code>.</li>
</ul>


<Story name="Footer Logo" parameters={{ backgrounds: { default: 'gray'}}}>
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <FooterLogo src={logo} src1={logowhite} headerText={caption.headerText} style={''} alt={'UNDP Logo'}></FooterLogo>;
  }}
</Story>

<Story name="Footer Logo White" parameters={{ backgrounds: { default: 'gray'}}}>
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <FooterLogo src={logowhite} headerText={caption.headerText} style={'white'} alt={'UNDP Logo'}></FooterLogo>;
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables, mixins from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component <code>footerLogo.scss</code></li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
</ul>
