import { Meta, Story } from "@storybook/addon-docs";
import { DownloadRow } from "./DownloadRow";

export const getCaptionForLocale = (locale) => {
  switch (locale) {
    case "english":
      const engText = {
        label: "Title of the publication quite long",
        label2: "PDF (1.2MB)",
        value: "category",
      };
      return engText;
    case "arabic":
      const arabicText = {
        label: "عنوان المنشور طويل جدا",
        label2: "PDF (1.2 ميجابايت)",
        value: "فئة",
      };
      return arabicText;
    case "burmese":
      const burmeseText = {
        label: "စာအုပ်ခေါင်းစဉ်က တော်တော်ရှည်တယ်။",
        label2: "PDF (1.2MB)",
        value: "အမျိုးအစား",
      };
      return burmeseText;
    case "japanese":
      const japaneseText = {
        label: "出版物のタイトルはかなり長い",
        label2: "PDF (1.2MB)",
        value: "カテゴリー",
      };
      return japaneseText;
    default:
      return {
        label: "Title of the publication quite long",
        label2: "PDF (1.2MB)",
        value: "category",
      };
  }
};

<Meta
  title="Testing/Download Row"
  argTypes={{
    type: {
      options: ["Checkbox", "Download", "External_link"],
      control: { type: "radio" },
      defaultValue: "Checkbox",
    },
  }}
/>

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 24px;
    color: #031c2d;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
`}</style>

# Download Row Component

Download row is comes with checkbox, download and external link controls.

<Story name="Download Row">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return (
      <DownloadRow
        title={caption.label}
        subtitle={caption.label2}
        value={caption.value} 
        {...args}
      ></DownloadRow>
    );
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
  <li>Take HTML from the HTML tab in canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>
    Include the common SCSS files like normalize, variables from{" "}
    <code>assets/scss</code>
  </li>
  <li>
    Component SCSS file: <code>downloadrow.scss</code>
  </li>
  <u>To use CSS include the following files in your implementation</u>
  <li>
    The compiled CSS file from <code>assets/css/style.css</code>
  </li>
</ul>
