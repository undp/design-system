import { Meta, Story } from '@storybook/addon-docs';
import { Page } from './Page'

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {header: 'Our Focus', country: 'The sdgs in action'}; return engText;
    case 'arabic': const arabicText = {header: 'تركيزنا', country: 'أهداف التنمية المستدامة في العمل'}; return arabicText;
    case 'burmese': const burmeseText = {header: 'ကျွန်ုပ်တို့၏အာရုံစူးစိုက်မှု', country: 'SDGS လုပ်ဆောင်ချက်'}; return burmeseText;
    case 'japanese': const japaneseText = {header: '私たちの焦点', country: '動作中のSDGS'}; return japaneseText;
    default:
      return {header: 'Our Focus', country: 'The sdgs in action'};
  }
};

<Meta title="Testing/Text/Page Block" component={Page} parameters={{
    backgrounds: {
      default: 'white',
      values: [
        { name: 'white', value: '#fff' },
        { name: 'gray', value: '#D4D6D8' },
      ],
    },
  }} />

<style>{`
  .sbdocs-h1 {
    font-weight: 700;
  }
  .subheading {
    font-weight: 500;
    font-size: 22px;
    color: #161616;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .high {
    font-weight: 500;
    font-size: 14px;
    color: #161616;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .docblock-code-toggle {
    display: none;
  }
`}</style>

# Page Title Block 

The page Block Component defines a section of information data with some items - Header title, Description. <code>H2</code> <code>H4</code> Atoms tags are used for creating this section.

<Story name="Page Block Black">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Page headerText={caption.header} countryText={caption.country} style={''}></Page>;
  }}
</Story>

<Story name="Page Block white" parameters={{ backgrounds: { default: 'gray'}}}>
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Page headerText={caption.header} countryText={caption.country} style={'white'} ></Page>;
  }}
</Story>

<div className="subheading">Usage</div>

<ul>
<li>Take HTML from the HTML tab in the canvas</li>
<u>To use SCSS include the following files in your implementation</u>
<li>Include the common SCSS files like breakpoints, variables, mixin from <code>assets/scss</code></li>
<li>Component SCSS file: <code>page.scss</code></li>
<u>To use CSS include the following files in your implementation</u>
<li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
