import { Meta, Story } from '@storybook/addon-docs';
import { ProgressBar } from './progress_bar'

<Meta title="Atoms/Reach Elements/ProgressBar" component={ProgressBar} />

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Progress Bar Tag component

The <code>progress</code> HTML element displays an indicator showing the completion progress of a task, typically displayed as a progress bar.

<Story name="Progress Bar 0">
  return <ProgressBar  value={0}></ProgressBar>;
</Story>

<Story name="Progress Bar 25">
  return <ProgressBar  value={25}></ProgressBar>;
</Story>

<Story name="Progress Bar 50">
  return <ProgressBar  value={50}></ProgressBar>;
</Story>

<Story name="Progress Bar 100">
  return <ProgressBar  value={100}></ProgressBar>;
</Story>


<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables, mixins, breakpoints from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component <code>progress.scss</code></li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
</ul>
