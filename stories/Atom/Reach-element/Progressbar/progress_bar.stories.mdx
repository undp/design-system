import { Meta, Story } from '@storybook/addon-docs';
import { ProgressBar } from './progress_bar'

<Meta title="Atoms/Reach Elements/ProgressBar" component={ProgressBar} />

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Progressbar Component

Progressbar provides a visual representation of a user’s progress on a page

<Story name="Progress Bar 0">
  return <ProgressBar  value={0}></ProgressBar>;
</Story>

<Story name="Progress Bar 25">
  return <ProgressBar  value={25}></ProgressBar>;
</Story>

<Story name="Progress Bar 50">
  return <ProgressBar  value={50}></ProgressBar>;
</Story>

<Story name="Progress Bar 100">
  return <ProgressBar  value={100}></ProgressBar>;
</Story>


<div className="subheading">Usage</div>
<ul>
<li>Take HTML from the HTML tab in canvas</li>
<u>To use SCSS include the following files in your implementation</u>
<li>Include the common SCSS files like normalize, variables from <code>assets/scss</code></li>
<li>Component SCSS file: <code>progress.scss</code></li>
<u>To use CSS include the following files in your implementation</u>
<li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
