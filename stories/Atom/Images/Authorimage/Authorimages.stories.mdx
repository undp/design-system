import { Meta, Story } from '@storybook/addon-docs';
import { Large } from './Large';
import { Small } from './Small';

<Meta title="Atoms/Image/Author Image"/>

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 24px;
    color: #031c2d;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
  div[id^="story--atoms-image-author-image"] {
    display: inline-block;
  }
`}</style>  

# Author Image Component
The Author Image Component used to show the author image in a related pages.

<Story name="Large">
    return <Large></Large> ;
</Story>

<Story name="Small">
    return <Small></Small> ;
</Story>

<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component <code>authorimage.scss</code></li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
<li>Author Images are saved in <code>assets/images/</code> folder in SVG, PNG, JPG, JPEG format.</li>
</ul>
