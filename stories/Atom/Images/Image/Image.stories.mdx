import { Meta, Story } from '@storybook/addon-docs';
import { Image } from './Image';

<Meta title="Atoms/Image/Images"/>

<style>{`
 .subheading {
    font-weight: 700;
    font-size: 24px;
    color: #031c2d;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
`}</style>  

# Image Component
The Image Component is used to embed an image in a web page.

<ul>
<li>Images are saved in <code>assets/images/</code> folder in SVG, PNG, JPG, JPEG format. </li>
<li>The <code>img</code> tag has two required attributes:</li>
  <ul>
    <li>src - Specifies the path to the image</li>
    <li>alt - Specifies an alternate text for the image</li>
  </ul>
</ul>

<Story name="Image">
    return <Image></Image> ;
</Story>

<div className="subheading">Usage</div>
<ul>
<li>Component wise SCSS file residing in each component <code>image.scss</code>.</li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code>.</li>
<li>We can use the <code>Image</code> in the any component by import the <code>Image atom</code> in the required code.</li>
</ul>
