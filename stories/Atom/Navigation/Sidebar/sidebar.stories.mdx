import { Meta, Story } from '@storybook/addon-docs';
import { SidebarFirstLevel } from './sidebarFirstLevel';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {blockquote: 'Page Title'}; return engText;
    case 'arabic': const arabicText = {blockquote: 'عنوان الصفحة'}; return arabicText;
    case 'burmese': const burmeseText = {blockquote: 'စာမျက်နှာခေါင်းစဉ်'}; return burmeseText;
    case 'japanese': const japaneseText = {blockquote: 'ページタイトル'}; return japaneseText;
    default:
      return {blockquote: 'Page Title'};
  }
};

<Meta title="Testing/Navigation/Sidebar" 
  argTypes={{
    variant: {
      options: ["default", "selected"],
      control: { type: "radio" },
      defaultValue: 'default',
    }
  }}
/>

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Sidebar Component
The sidebar component is used for showing the nested mapping of the related pages. it shows the first level of the sidebar.

<Story name="Sidebar">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <SidebarFirstLevel text={caption.blockquote} {...args}></SidebarFirstLevel>;
  }}
</Story>


<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables, mixins, breakpoints from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component</li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
<li>For attaching the JS library, call the function on id 'test' given in the HTML</li>
</ul>
