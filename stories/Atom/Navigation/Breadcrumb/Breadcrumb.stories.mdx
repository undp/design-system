import { Meta, Story } from '@storybook/addon-docs';
import { Breadcrumb } from './Breadcrumb';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail: 'page title'}; return engText;
    case 'arabic': const arabicText = {detail: 'عنوان الصفحة'}; return arabicText;
    case 'burmese': const burmeseText = {detail: 'စာမျက်နှာခေါင်းစဉ်'}; return burmeseText;
    case 'japanese': const japaneseText = {detail: 'ページタイトル'}; return japaneseText;
    default:
      return {detail: 'page title'};
  }
};

<Meta title="Atoms/Navigation/Breadcrumb" />

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
   .subheading2 {
    font-weight: 700;
    font-size: 14px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
  }  
`}</style>


# Breadcrumb Component
The breadcrumb is a secondary navigation pattern that helps a user understand the hierarchy among levels and navigate back through them.

Breadcrumbs show users their current location relative to the information architecture and enable them to quickly move up to a parent level or previous step.


<Story name="Breadcrumb">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Breadcrumb text={caption.detail} style={'first'}></Breadcrumb>;
  }}
</Story>


<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables, mixins from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component <code>breadcrumb.scss</code></li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
</ul>
