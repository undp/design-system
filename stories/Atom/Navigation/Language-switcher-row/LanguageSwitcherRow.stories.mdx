import { Meta, Story } from '@storybook/addon-docs';
import { LanguageSwitcherRow } from './LanguageSwitcherRow';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail: 'Français'}; return engText;
    case 'arabic': const arabicText = {detail: 'الفرنسية'}; return arabicText;
    case 'burmese': const burmeseText = {detail: 'Français'}; return burmeseText;
    case 'japanese': const japaneseText = {detail: 'フランス語'}; return japaneseText;
    default:
      return {detail: 'Français'};
  }
};

<Meta title="Testing/Navigation/Language Row" component={LanguageSwitcherRow} />

<style>{`
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>


# Language Switcher Row Component
Language Switcher Row is used to show the language option in the language switcher component in the header component.

<Story name="Language Row">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <LanguageSwitcherRow text={caption.detail} ></LanguageSwitcherRow>;
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
<li>We can take HTML from the HTML tab in the canvas tab</li>
<li>Include the common SCSS file like normalize, variables, mixins from the folder <code>assets/scss</code></li>
<li>Component wise SCSS file residing in each component <code>LanguageSwitcherRow.css</code></li>
<li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
</ul>
