import { Meta, Story } from "@storybook/addon-docs";
import { Label } from "./Label";

export const getCaptionForLocale = (locale) => {
  switch (locale) {
    case "english":
      const engText = {
        detail1: "Form Label:",
        detail2: "*Error: this field is required",
        detail8: "8 characters minimum",
      };
      return engText;
    case "arabic":
      const arabicText = {
        detail1: "تسمية النموذج:",
        detail2: "* خطأ: هذا الحقل مطلوب",
        detail8: "8 أحرف كحد أدنى",
      };
      return arabicText;
    case "burmese":
      const burmeseText = {
        detail1: "ပုံစံတံဆိပ်:",
        detail2: "*အမှား - ဤအကွက်လိုအပ်သည်",
        detail8: "အနည်းဆုံးစာလုံး ၈ လုံး",
      };
      return burmeseText;
    case "japanese":
      const japaneseText = {
        detail1: "フォームラベル：",
        detail2: "*エラー：このフィールドは必須です",
        detail8: "最小8文字",
      };
      return japaneseText;
    default:
      return {
        detail1: "Form Label:",
        detail2: "*Error: this field is required",
        detail8: "8 characters minimum",
      };
  }
};

<Meta title="Testing/Label" />

<style>{`
  #story--atoms-input-group--text-default, #story--atoms-input-group--text-focus, #story--atoms-input-group--text-error, #story--atoms-input-group--text-disabled,#story--atoms-input-group--number-default, #story--atoms-input-group--number-error, #story--atoms-input-group--number-focus, #story--atoms-input-group--number-error, #story--atoms-input-group--number-disabled, #story--atoms-input-group--password-default, #story--atoms-input-group--password-focus, #story--atoms-input-group--password-error, #story--atoms-input-group--password-disabled, #story--atoms-input-group--telephone-default, #story--atoms-input-group--telephone-focus, #story--atoms-input-group--telephone-error, #story--atoms-input-group--telephone-disabled, #story--atoms-input-group--date-default, #story--atoms-input-group--date-focus, #story--atoms-input-group--date-error,#story--atoms-input-group--help-message, #story--atoms-input-group--error-message {
    display: none;
  }
  .sbdocs.sbdocs-div.input_group_wrapper.css-14m39zm {
    display: flex;
    flex-wrap: 1;
    justify-content: space-around;
    margin-bottom: 40px;
    box-shadow: 0 1px 8px rgb(0 0 0 / 15%);
  }
  .sbdocs.sbdocs-div.input__code.css-14m39zm {
    margin: 10px;
  }
  .sbdocs.sbdocs-div.input__label.css-14m39zm {
    text-align: center;
    text-decoration: underline;
  }
  .sbdocs.sbdocs-div.input_base-wrapper.css-14m39zm {
    display: inline-flex;
    align-items: baseline;
    box-shadow: 0 1px 8px rgb(0 0 0 / 15%);
  }
  .sbdocs.sbdocs-div.base_item.css-14m39zm {
    margin-left: 20px;
  }

  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Label Component

The Label component defines a labels of input - Text, Error and Help.

<div className="subheading"> Base Label </div>

<Story name="Labeltext">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return (
      <Label labelText={caption.detail1} State="default" {...args}></Label>
    );
  }}
</Story>

<Story name="Label-Help">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Label helpText={caption.detail8} State="default" {...args}></Label>;
  }}
</Story>

<Story name="Label-Error">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Label errorText={caption.detail2} State="error" {...args}></Label>;
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
  <li>Take HTML from the HTML tab in the canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>
    Include the common SCSS files like mixin <code>assets/scss</code>
  </li>
  <li>
    Component SCSS file <code>input_group.scss</code>
  </li>
  <u>To use CSS include the following files in your implementation</u>
  <li>
    The compiled CSS file from <code>assets/css/style.css</code>
  </li>
</ul>
