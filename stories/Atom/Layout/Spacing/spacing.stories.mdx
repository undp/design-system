import { Meta, Story } from '@storybook/addon-docs';
import { Spacing } from './spacing';

<Meta title="Foundation/Layout/Spacing" component={Spacing} parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true }
    }
  }}/>

<style>{`
  div#story--atoms-spacing--spacing {
    box-shadow: 0 1px 8px rgb(0 0 0 / 15%);
    box-sizing: inherit;
  }
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Spacing Component
Space units create visual rhythm and balance.

The following units can be applied to margin or padding properties, vertically or horizontally.

<Story name="Spacing">
  return <Spacing></Spacing>;
</Story>

### Usage

- Take HTML from the HTML tab in canvas
- <u>To use SCSS include the following files in your implementation</u>
- Include the common SCSS files like normalize, variables from <code>assets/scss</code>
- Component SCSS file: <code>spacing.scss</code>
- <u>To use CSS include the following files in your implementation</u>
- The compiled CSS file from <code>assets/css/style.css</code>
