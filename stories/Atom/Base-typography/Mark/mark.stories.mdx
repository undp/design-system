import { Meta, Story } from '@storybook/addon-docs';
import { Mark } from './mark';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail: 'Eos nulla molestiae'}; return engText;
    case 'arabic': const arabicText = {detail: 'Eos nulla molestiae'}; return arabicText;
    case 'burmese': const burmeseText = {detail: 'Eos nulla molestiae ဖြစ်သည်'}; return burmeseText;
    case 'japanese': const japaneseText = {detail: '不快感なし'}; return japaneseText;
    default:
      return {detail: 'Eos nulla molestiae'};
  }
};

<Meta title="Atoms/Base Typography/Mark" component={Mark} />

<style>{`
  div#story--atoms-base-typography-mark--mark {
    box-shadow: 0 1px 8px rgb(0 0 0 / 15%);
    box-sizing: inherit;
    width: 100%;
    padding: 10px;
  }
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Mark Component
The Mark component define the marked text. It is used to highlight the part of the text in a paragraph.
<ul>
  <li>We can use the <code>mark</code> in any component to render the text found within the <code>mark tag</code> as text with a light yellow background color.</li>
</ul>

Typographic scale when using mark in text component:

<Story name="Mark">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Mark label={caption.detail} ></Mark>;
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
  <li>Include the common SCSS file like normalize, variables, mixins from the folder <code>assets/scss</code></li>
  <li>Component wise SCSS file residing in each component<code>mark.scss</code></li>
  <li>Include the Compiled CSS file from the <code>assets/css/style.css</code></li>
  <li>For attaching the JS library, call the function on id 'test' given in the HTML</li>
</ul>
