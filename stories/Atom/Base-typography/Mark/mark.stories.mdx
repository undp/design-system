import { Meta, Story } from '@storybook/addon-docs';
import { Mark } from './mark';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail: 'Eos nulla molestiae'}; return engText;
    case 'arabic': const arabicText = {detail: 'لا إزعاج'}; return arabicText;
    case 'burmese': const burmeseText = {detail: 'အဆင်မပြေတာမရှိဘူး'}; return burmeseText;
    case 'japanese': const japaneseText = {detail: '不快感なし'}; return japaneseText;
    default:
      return {detail: 'Eos nulla molestiae'};
  }
};

<Meta title="Testing/Base Typography/Mark" component={Mark} />

<style>{`
  div#story--atoms-base-typography-mark--mark {
    box-shadow: 0 1px 8px rgb(0 0 0 / 15%);
    box-sizing: inherit;
    width: 100%;
    padding: 10px;
  }
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Mark Component
The Mark component define the marked text. It is used to highlight the part of the text in a paragraph.
<ul>
  <li>We can use the <code>mark</code> in any component to render the text found within the <code>mark tag</code> as text with a light yellow background color.</li>
</ul>

Typographic scale when using mark in text component:

<Story name="Mark">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Mark label={caption.detail} ></Mark>;
  }}
</Story>

T<div className="subheading">Usage</div>
<ul>
  <li>Take HTML from the HTML tab in canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>Include the common SCSS files like normalize, variables, mixins from <code>assets/scss</code></li>
  <li>Component SCSS file:<code>mark.scss</code></li>
  <u>To use CSS include the following files in your implementation</u>
  <li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
