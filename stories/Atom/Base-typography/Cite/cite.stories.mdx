import { Meta, Story } from '@storybook/addon-docs';
import { Cite } from './cite';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail: '-Firstname Lastname'}; return engText;
    case 'arabic': const arabicText = {detail: 'الاسم الاول الاسم الاخير-'}; return arabicText;
    case 'burmese': const burmeseText = {detail: '-နာမည် မျိုးနွယ်အမည်'}; return burmeseText;
    case 'japanese': const japaneseText = {detail: '-名前苗字'}; return japaneseText;
    default:
      return {detail: '-Firstname Lastname'};
  }
};

<Meta title="Testing/Base Typography/Cite" component={Cite} />

<style>{`
  div#story--atoms-base-typography-cite--cite {
    box-shadow: 0 1px 8px rgb(0 0 0 / 15%);
    box-sizing: inherit;
    width: 100%;
    padding: 10px;   
  }
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }  
`}</style>

# Cite Component
The Cite component defines the title of a creative work (e.g. a book, a poem, a song, a movie, a painting, a sculpture, etc.).
<ul>
  <li>We can use <code>cite</code> in any component to render the text found within the <code>cite tag</code>.</li>
</ul>

Typographic scale when using cite in text component:

<Story name="Cite">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Cite label={caption.detail} ></Cite>;
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
  <li>Take HTML from the HTML tab in canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>Include the common SCSS files like normalize, variables, mixins from <code>assets/scss</code></li>
  <li>Component SCSS file:<code>cite.scss</code></li>
  <u>To use CSS include the following files in your implementation</u>
  <li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
