import { Meta, Story } from '@storybook/addon-docs';
import Select from './Select';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = 'Select Language'; return engText;
    case 'arabic': const arabicText = 'اختار اللغة'; return arabicText;
    case 'burmese': const burmeseText = 'ဘာသာစကားကို ရွေးပါ။'; return burmeseText;
    case 'japanese': const japaneseText = '言語を選択する'; return japaneseText;
    default: return 'Category';
  }
};

<Meta title="Components/Forms/Select" />

<style>{`
 .subheading {
    font-weight: 700;
    font-size: 24px;
    color: #031c2d;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
`}</style>

# Select
The select component allows users to choose multiples option from a list. It is used in forms when there are multiple choices that can be collapsed under one title.

<Story name="Select">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <Select text={caption}></Select>;
  }}
</Story>





<div className="subheading">Usage</div>
<ul>
<li>Take HTML from the HTML tab in the canvas</li>
<li>Take the multiselect js files from <code>assets/js/multiselect.js</code> and <code>assets/js/multiselect-init.js</code></li>
<li><u>To use SCSS include the following files in your implementation</u></li>
<li>Include the common SCSS files like normalize, variables from <code>assets/scss</code></li>
<li>Component SCSS file: <code>multiselect.scss</code></li>
<li><u>To use CSS include the following files in your implementation</u></li>
<li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
