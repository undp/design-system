import { Meta, Story } from '@storybook/addon-docs';
import { Ctalink } from './CtaLink';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = {detail1: 'READ MORE'}; return engText;
    case 'arabic': const arabicText = {detail1: 'اقرأ أكثر'}; return arabicText;
    case 'burmese': const burmeseText = {detail1: 'ပိုပြီးဖတ်ပါ'}; return burmeseText;
    case 'japanese': const japaneseText = {detail1: '続きを読む'}; return japaneseText;
    default:
    return {detail1: 'READ MORE'};
  }
};

<Meta title="Components/UI components/Buttons/CTA Link" 
  argTypes={{
    Type: {
      options: ["Expanding Arrow", "Space"],
      control: { type: "inline-radio" },
      defaultValue: 'Expanding Arrow'
    },
    Tag: {
      options: ['Hyperlink', 'Inline'],
      control: { type: 'inline-radio' },
      defaultValue: 'Hyperlink'
    },
  }}
/>

<style>{`
div#story--atoms-buttons--CtaLink-arrow, div#story--atoms-buttons--CtaLink-space, div#story--atoms-buttons-link--CtaLink {
    display: none;
  }
  .cta_wrapper {
    display: flex;
  }
  .cta__label {
    padding-right: 20px;
  }
  .cta_item {
    padding: 10px;
  }
  .subheading {
    font-weight: 700;
    font-size: 18px;
    color: #666;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
`}</style>

# CTA Link component
A cta link is a prompt on a website that tells the user to take some specified action.

<div className="cta_wrapper">
  <div className="cta_item">
    <div className="cta__box CtaLink-arrow"></div>
    <div className="cta__label">Cta link arrow expand</div>
    <div className="cta__code"> <Ctalink label="READ MORE" Type='Arrow'></Ctalink></div>
  </div>
</div>
<div className="cta_wrapper">
  <div className="cta_item">
    <div className="cta__box CtaLink-space"></div>
    <div className="cta__label">Cta link with space expand </div>
    <div className="cta__code"> <Ctalink label="READ MORE" Type='Space'></Ctalink></div>
  </div>
</div>

<Story
  name="CTA Link"
>
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    // replace key
    args['button_option'] = args['Tag'];
    delete args['Tag'];
    return (
      <Ctalink
        label={caption.detail1}
        {...args}
      ></Ctalink>
    );
  }}
</Story>


<div className="subheading">Usage</div>
<ul>
  <li>Take HTML from the HTML tab in canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>Include the common SCSS files like normalize, variables, mixins from <code>assets/scss</code></li>
  <li>Component SCSS file:<code>CtaLink.scss</code></li>
  <u>To use CSS include the following files in your implementation</u>
  <li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
