import { Meta, Story } from '@storybook/addon-docs';
import {ImageOnlyCarousel} from './ImageOnlyCarousel';
import fluidCarouselImg from '../../../../assets/images/carousel3-img.jpg';


export const statsArray = (locale) => {
  switch(locale) {
    case 'english': const engText = {data: [{
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    }]}; return engText.data;
    case 'arabic': const arabicText = {data: [{
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    }]}; return arabicText.data;
    case 'burmese': const burmeseText = {data: [{
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    }]}; return burmeseText.data;
    case 'japanese': const japaneseText = {data: [{
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    }]}; return japaneseText.data;
    default:
    const dummy =  {data: [{
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    },
    {
      slideImage: fluidCarouselImg,
    }]};
    return dummy.data
  }
};

<Meta title="Components/UI components/Carousel/Image Carousel"/>

<style>{`
  .subheading {
    font-weight: 600;
    font-size: 20px;
    color: #333333;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 12px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
  .sbdocs .sbdocs-h2 {
    border-bottom: none;
  }
  div[id^="story--molecules-text-authors"] {
    padding: 20px;
    box-shadow: rgb(0 0 0 / 10%) 0px 1px 3px 0px;
    border: 1px solid rgba(0, 0, 0, 0.1);
    margin: 25px 0px 40px;
    border-radius: 4px;
  }
`}</style>

# Image only carousel component

## Overview

The Image only carousel Component is created for showing slider, designation, and image in particular section. It is a multi-usage component that creates are usually teasing some kind of content.

## Default Representation & various possible states

<Story name="Image Carousel">
  {(args, { globals: { locale } }) => {
    const caption = statsArray(locale);
    return <ImageOnlyCarousel data={caption}></ImageOnlyCarousel>;
  }}
</Story>

## Dependencies

Not Applicable

## Usage

<ul>
  <li>Take HTML from the HTML tab in the canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>
    Include the common SCSS files like variables, mixin from{" "}
    <code>assets/scss</code>
  </li>
  <li>
    Component SCSS file: <code>imageonlycarousel.scss</code>
  </li>
  <u>To use CSS include the following files in your implementation</u>
  <li>
    The compiled CSS file from <code>assets/css/style.css</code>
  </li>
  <u>To use JS include the following files in your implementation</u>
  <li>
    Include JS Slider library file  from <code>assets/js/swiper.js</code>
  </li>
  <li>
    The compiled JS file from <code>assets/js/viewport.js</code>
  </li>
</ul>
