import { Meta, Story, canvas } from '@storybook/addon-docs';
import { Modal } from './Modal';

<Meta title="Components/UI components/Modal" />

<style>{`
 .subheading {
    font-weight: 700;
    font-size: 20px;
    color: #031c2d;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 15px;
  }
  .subheading2 {
    font-weight: 700;
    font-size: 16px;
    color: #031c2d;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 10px;
    text-decoration: underline;
  }
`}</style>

# Modal Card Component

The Modal component defines a block of information data with some.

<Story name="Modal" >
  return <Modal></Modal>;
</Story>

<div className="subheading">Usage</div>
<ul>
<li>Take HTML from the HTML tab in the canvas</li>
<u>To use SCSS include the following files in your implementation</u>
<li>Include the common SCSS files like variables, mixin from <code>assets/scss</code></li>
<li>Component SCSS file: <code>modal.scss</code></li>
<u>To use CSS include the following files in your implementation</u>
<li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
