import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { ImageRevelCard } from './ImageCard';
import img from '../../../../assets/images/cardimage.jpg';
import imgsmall from '../../../../assets/images/CardImgsmall.jpg';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = { name: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ', btnlabel: 'READ MORE'}; return engText;
    case 'arabic': const arabicText = { name: 'الألم هو الحب نفسه', btnlabel: 'اقرأ أكثر'}; return arabicText;
    case 'burmese': const burmeseText = { name: 'နာကျင်မှုကအချစ်ပါဘဲ', btnlabel: 'ပိုပြီးဖတ်ပါ'}; return burmeseText;
    case 'japanese': const japaneseText = { name: '痛みは愛そのものです', btnlabel: '続きを読む'}; return japaneseText;
    default:
      return { name: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ', btnlabel: 'READ MORE'};
  }
};

<Meta 
  title="Components/UI components/Cards" 
  argTypes={{
    Size: {
      options: ['Large', 'Small'],
      control: { type: 'inline-radio' },
      defaultValue: 'Large'
    }
  }}/>

# Image Reveal Card
Image reveal cards are cards with information about the image and CTA link for detailed information.

### Overview
The Image reveal card component is a multi-usage component which creates boxes that are usually teasing some kind of content like- Image, Text and CTA Link. 

#### When to use:
- When text and CTA Link needs to be attached with an image.

### Formatting
#### Default
It consists of a grid with an image, text and cta link.

### Content
There are two types of image reveal card: Medium and Small
1. Medium: Image Reveal Card of medium size shows the Image and CTA Link. The Text will show on hover.
2. Small: Image Reveal Card of small size comes with a different Image and CTA Link. The Text will show on hover.

### Behaviors
#### States
There are 2 states in the Image Reveal Card: Default and Hover.
**Default:** It consists of Image and CTA Link.
**Hover:** It consists of Image, Text and CTA Link. It changes appearance when the mouse hovers over it.

<Canvas>
  <Story name="Image Reveal Cards">
    {(args, { globals: { locale } }) => {
      const caption = getCaptionForLocale(locale);
      return <ImageRevelCard contentname={caption.name} image={img} button={caption.btnlabel} link='#' {...args}> </ImageRevelCard>;
    }}
  </Story>
</Canvas>

### CSS and JS References [ADDED BY DEVs]

#### CSS:
Add the base layout style from dist/css/base-minimal.css
- dist/css/components/[component].css

#### JS:
- dist/js/[component].js

### Interactions
Hover state applies to the entire label.
After clicking on the label, the page is redirected to the mentioned link.

### Changelog
1.0 — Released component
