import { Meta, Story, Canvas, Source } from '@storybook/addon-docs';
import { ImageRevelCard } from './ImageCard';
import img from '../../../../assets/images/cardimage.jpg';
import imgsmall from '../../../../assets/images/CardImgsmall.jpg';

export const getCaptionForLocale = (locale) => {
  switch(locale) {
    case 'english': const engText = { 
      takeactiondata: [
        {
        contentname: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ',
        btnlabel: 'READ MORE',
        image:img,
        imgalt:img,
        link:'#',
        size:'medium-4',
        }
      ]
    }; return engText;
    case 'arabic': const arabicText = { 
      takeactiondata: [
        {
          name: 'الألم هو الحب نفسه', 
          btnlabel: 'اقرأ أكثر',
          image:img,
          imgalt:img,
          link:'#',
          size:'medium-4',
        }
      ]
    }; return arabicText;
    case 'burmese': const burmeseText = { 
      takeactiondata: [
        {
          name: 'နာကျင်မှုကအချစ်ပါဘဲ', 
          btnlabel: 'ပိုပြီးဖတ်ပါ',
          image:img,
          imgalt:img,
          link:'#',
          size:'medium-4',
        }
      ]
    }; return burmeseText;
    case 'japanese': const japaneseText = { 
      takeactiondata: [
        {
          name: '痛みは愛そのものです', 
          btnlabel: '続きを読む',
          image:img,
          imgalt:img,
          link:'#',
          size:'medium-4',
        }
      ]
    }; return japaneseText;
    default:
      return { 
         takeactiondata: [
        {
          name: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ', 
          btnlabel: 'READ MORE',
          image:img,
          imgalt:img,
          link:'#',
          size:'medium-4',
        }
      ]
    };
  }
};

<Meta title="Components/UI components/Cards/Image Reveal CTA Card" />

<style>{`
 .subheading {
    font-weight: 700;
    font-size: 24px;
    color: #031c2d;
    letter-spacing: 1.5px;
    line-height: 24px;
    margin-bottom: 12px;
    margin-top: 40px;
  }
  .sbdocs .sbdocs-p,
  .sbdocs .sbdocs-li{
    font-size: 1rem;
  }
`}</style>

# Image Reveal Card Component
The Image reveal card component multi usage component which creates boxes that are usually teasing some kind of content like - Image, Text, and CTA Link. We are using the grid system here <code>assets/scss/grid.scss</code>. Atoms tags are used for creating this card.

<div className="subheading">Image Reveal Card- Medium</div>
<p>Image Reveal Card of medium size are shows the Image, and CTA Link. The text will show on hover.</p>

<Story name="Image card">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <ImageRevelCard 
      data={caption.takeactiondata} {...args}></ImageRevelCard>;
  }}
</Story>

<div className="subheading">Image Reveal Card- Small</div>
<p>Image Reveal Card of small size comes with a different image, and CTA Link. The text will show on hover.</p>

<Story name="Image card-small">
  {(args, { globals: { locale } }) => {
    const caption = getCaptionForLocale(locale);
    return <ImageRevelCard  data={caption.takeactiondata} {...args}></ImageRevelCard>;
  }}
</Story>

<div className="subheading">Usage</div>
<ul>
  <li>Take HTML from the HTML tab in the canvas</li>
  <u>To use SCSS include the following files in your implementation</u>
  <li>Include the common SCSS files like variables, mixin from <code>assets/scss</code></li>
  <li>Component SCSS file: <code>imagecard.scss</code></li>
  <u>To use CSS include the following files in your implementation</u>
  <li>The compiled CSS file from <code>assets/css/style.css</code></li>
</ul>
